# Search and address TODO
# TODO: Define families and reuse in fgeo.tool

fgeo.base::suffix_match()
fgeo.base::to_tidy_names()
fgeo.base::glue_pipe()
fgeo.base::enline()

# Approximate
fgeo.base::round_any()
fgeo.base::guess_max()
fgeo.base::guess_plotdim()

# Restructure data (general)
fgeo.base::gather_mat()
fgeo.base::gather_mats()

# Find / search (general)
fgeo.base::find_datasets()

# Names
fgeo.base::name_dfs()
fgeo.base::nms_extract_match()

# git ---------------------------------------------------------------------

# From .R send to terminal: control + alt + enter
git checkout master
git merge dev
git pull
git push
git checkout dev
